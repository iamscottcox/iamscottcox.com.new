<div class="card box-shadow hoverable">
    {% if post.img %}
        <div class="card-image">
            {{ post.img }}
        </div>
    {% endif %}
    <div class="card-block">
        <h2>{{ post.title }}</h2>
        {{ post.get_preview(40) }}
    </div>
</div>