<div class="card card-row box-shadow hoverable">
    <div class="card-image">
        {{ post.image }}
    </div>
    <div class="card-block">
        <h2>{{ post.title }}</h2>
        {{ post.get_preview(40) }}
    </div>
</div>
