<div class="container">
    <div class="row">
        {% for post in posts %}
            <div class="col-md-4">
                <div class="card">
                    {{ post.title }}
                    {{ post.content }}
                </div>
            </div>
        {% endfor %}
    </div>
</div>