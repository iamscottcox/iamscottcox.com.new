<div class="card card-row box-shadow hoverable">
    <div class="card-image">
        {{ post.image }}
    </div>
    <div class="card-block">
        <a href="{{ post.link }}">
            <h2>{{ post.title }}</h2>
        </a>
        {{ post.get_preview(40) }}
    </div>
</div>
